name: "Base Setup"
description: "Set up the base environment"
inputs:
  node-version:
    description: "Node.js version"
    required: true
  python-version:
    description: "Python version"
    required: true
  cache-prefix:
    description: "Cache prefix to use for Foundry cache"
    required: false
    default: "default"

runs:
  using: "composite"
  steps:
    - name: Cache Dependencies
      uses: buildjet/cache@v4
      with:
        path: ./dependencies
        key: ${{ runner.os }}-dependencies-${{ hashFiles('./soldeer.lock') }}

    - uses: pnpm/action-setup@v4
      with:
        version: 9

    - name: Install Node.js
      uses: buildjet/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - name: Install Python
      uses: buildjet/setup-python@v5
      if: ${{ inputs.python-version != '' }}
      with:
        python-version: ${{ inputs.python-version }}
        cache: pip

    - name: Install Python dependencies
      if: ${{ inputs.python-version != '' }}
      run: pip install -r requirements.txt
      shell: bash

    - name: Install Foundry
      uses: foundry-rs/foundry-toolchain@v1
      with:
        version: nightly
        cache-key: ${{ github.job }}-${{ inputs.cache-prefix }}-${{ github.sha }}

    - name: Install Soldeer dependencies
      run: forge soldeer install
      shell: bash

    - name: Install dependencies
      run: pnpm install
      shell: bash
